# Dependencies / build output (never ship in git or docker context)
node_modules
dist
coverage
*.log
.DS_Store

# Secrets
.env
.env.local
.env.*.local

# Local package manager noise
pnpm-lock.yaml
pnpm-workspace.yaml
.npmrc

# IDE
.vscode/
.idea/

# Accidental shell/docker scrap filenames — do not match with bare "[" (breaks dockerignore globs)
reading
resolve
transferring
CACHED
ERROR
